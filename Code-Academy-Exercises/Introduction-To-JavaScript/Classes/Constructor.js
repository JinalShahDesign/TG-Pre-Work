class Surgeon {
  constructor(name, department) {
    this._name = name;
    this._department = department;
  }
}

/*A major difference between syntax for a class and objects is the method constructor () By convention we capitalize and CamelCase class names.
JS will invoke the constructor method every time a new instance is created for the class. */
