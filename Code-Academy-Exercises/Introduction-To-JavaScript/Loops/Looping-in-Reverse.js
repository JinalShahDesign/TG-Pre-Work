// The loop below loops from 0 to 3. Edit it to loop backwards from 3 to 0
for (let counter = 3; counter >= 0; counter--){
  console.log(counter)
};

/*To run a backward for loop set the iterator variable to the highest desired value.
Set the stopping condition to the lowest desired amount.The iterator should decrease in intervals after each iteration.*/
