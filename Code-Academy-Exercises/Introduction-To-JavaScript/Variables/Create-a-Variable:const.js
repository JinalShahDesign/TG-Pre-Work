const entree = 'Enchiladas';

console.log(entree);

const testing;

//The const keyword is similar to var and let but you can't reassign a const variable. If you try to reassign a const variable you'll get a TypeError.
