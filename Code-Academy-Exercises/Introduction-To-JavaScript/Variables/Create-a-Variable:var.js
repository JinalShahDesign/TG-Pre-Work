var favoriteFood = 'pizza';
var numOfSlices = 8;
console.log(favoriteFood);
console.log(numOfSlices);

//var creates(declares)a new variable.
//It is convention to use camel case. The first word is lower case, and then every following word has a letter uppercased. = is the assignment operator. console.log prints the value within a variable. Variables cannot be the same as keywords.
