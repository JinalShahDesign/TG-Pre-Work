let newVariable = 1;
console.log( typeof newVariable);
//It's useful to track what data types of variables are in your program. The typeof operator checks the value to its right and returns or passes back , a string of data type.
