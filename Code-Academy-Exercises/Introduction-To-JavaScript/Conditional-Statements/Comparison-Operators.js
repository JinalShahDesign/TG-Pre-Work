let hungerLevel = 7;

if (hungerLevel > 7) {
  console.log('Time to eat!');
} else {
  console.log('We can eat later!');
};

//Sometimes we need to use different types of operators to compare values. These are called comparison operators. //(<, >, <=, >=, ===. !==,) Comparison operators compare the value on the left with the value on the right. All comparison statements evaluate to either true or false. 
