const fruits = ['mango', 'papaya', 'pineapple', 'apple'];

// Iterate over fruits below
fruits.forEach(fruit => console.log(`I want to eat a ${fruit}.`))

/*.forEach will execute the same code for each element of an array. A callback function is a function passed as an argument into another function. .forEach() loops through the array and executes the callback gunction for each element.
The return value for .forEach() will always be undefined. You can also use => to pass a callback for .forEach()*/
