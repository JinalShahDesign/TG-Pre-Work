const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('finish homework', 'go to the gym');

console.log(chores);

/*The push method allows us to add items to the end of an array. */
