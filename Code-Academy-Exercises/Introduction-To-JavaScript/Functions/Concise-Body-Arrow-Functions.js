const plantNeedsWater = day => day === 'Wednesday' ? true : false;

//Function that take only single parameters do not need the parameter to be enclosed in parentheses. But if a function takes 0 or multiple parameters then a parantheses is needed. A function body composed of a single-line block does not need curly braces. When you don't use the curly braces there is no need to have the return keyword (implicit return).
