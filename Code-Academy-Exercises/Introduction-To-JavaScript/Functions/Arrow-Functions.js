const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};

//arrow functions syntax is a shorter way to write functions using hte special "fat arrow" () => notation. Arrow functions replace typing the keyword function
