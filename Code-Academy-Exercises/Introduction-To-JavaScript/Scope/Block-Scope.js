const logVisibleLightWaves = () => {
 const lightWaves= 'Moonlight'
 console.log(lightWaves);
};
logVisibleLightWaves();
console.log(lightWaves);

//When a variable is defined inside a block, it is only accessible to the code within the curly braces. This is referred to as block scope.They are known as local variables. If you try to use local variables outside the function it will return with a ReferenceError.
